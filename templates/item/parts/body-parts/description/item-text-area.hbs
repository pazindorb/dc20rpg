<div class="description-column">
  <div class="text-box flavor-text">
    <label class="section-label">Flavor Text</label>
    {{editor enriched.flavor target="system.flavorText" button=true editable=editable class="min-h"}}
  </div>
  <div class="text-box description-text">
    <label class="section-label">Description</label>
    {{#if system.statuses}}
      {{#if system.statuses.identified}} 
        {{editor enriched.description target="system.description" button=true editable=editable}}
      
        {{#if system.outcome.success}}
        <div class="extra-description">
          <label>On Success:&nbsp</label>
          <span>{{system.outcome.success}}</span>
        </div>
        {{/if}}
        {{#if system.outcome.fail}}
        <div class="extra-description">
          <label>On Fail:&nbsp</label>
          <span>{{system.outcome.fail}}</span>
        </div>
        {{/if}}
        {{#if system.outcome.heavy}}
        <div class="extra-description">
          <label>On Heavy Hit:&nbsp</label>
          <span>{{system.outcome.heavy}}</span>
        </div>
        {{/if}}
        {{#if system.outcome.brutal}}
        <div class="extra-description">
          <label>On Brutal Hit:&nbsp</label>
          <span>{{system.outcome.brutal}}</span>
        </div>
        {{/if}}

      {{else}}
        <span class="unidentified"><br>UNIDENTIFIED</span>
      {{/if}}
    {{else}}
    {{editor enriched.description target="system.description" button=true editable=editable}}
    
      {{#if system.outcome.success}}
      <div class="extra-description">
        <label>On Success:&nbsp</label>
        <span>{{system.outcome.success}}</span>
      </div>
      {{/if}}
      {{#if system.outcome.fail}}
      <div class="extra-description">
        <label>On Fail:&nbsp</label>
        <span>{{system.outcome.fail}}</span>
      </div>
      {{/if}}
      {{#if system.outcome.heavy}}
      <div class="extra-description">
        <label>On Heavy Hit:&nbsp</label>
        <span>{{system.outcome.heavy}}</span>
      </div>
      {{/if}}
      {{#if system.outcome.brutal}}
      <div class="extra-description">
        <label>On Brutal Hit:&nbsp</label>
        <span>{{system.outcome.brutal}}</span>
      </div>
      {{/if}}

    {{/if}}
  </div>
</div>